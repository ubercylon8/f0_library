# Test Information: Nightmare-Eclipse RedSun Cloud Files Rewrite Primitive Chain

## Identification

- **UUID**: `0d7e7571-45e2-426a-ac8e-bdb000439761`
- **Category**: `intel-driven`
- **Subcategory**: `apt`
- **Target**: `windows-endpoint`
- **Threat Actor**: Nightmare-Eclipse
- **Source**: RedSun PoC, https://github.com/Nightmare-Eclipse/RedSun
- **Author**: sectest-builder
- **Created**: 2026-04-24
- **Re-scored under v2**: 2026-04-25
- **Version**: 2.0.0
- **Rubric version**: v2 (tiered, realism-first)

## Test Score: 7.0/10

> **Pre-lab score.** v2's Detection-Rule Firing Fidelity (sub-dimension 2c) is
> capped at 0.5 until lab evidence lands. Post-lab projection: ~9.0/10 once
> ≥80% rule firing is documented.

### Score Breakdown (Rubric v2)

| Tier | Sub-dimension | Score | Rationale |
|---|---|---|---|
| **1** | Safety Gate | **PASS** | All writes confined to `ARTIFACT_DIR` / `LOG_DIR`. Stage 4 has a HARD pre-check on TieringEngineService state — `CoCreateInstance` is SKIPPED if the service is not STOPPED, preventing inadvertent service-start. Post-activation safety check verifies the state didn't change. All reparse points sandbox→sandbox; never reference real system paths. All oplocks released within stage. No service start/stop ever attempted. |
| **2a** | API Fidelity (0–3) | **2.7** | Stages 1–3 mirror PoC API sequence (`CfRegisterSyncRoot`, `NtOpenDirectoryObject`/`NtQueryDirectoryObject`, `FSCTL_REQUEST_BATCH_OPLOCK`, `FSCTL_SET_REPARSE_POINT`, `FSCTL_GET_REPARSE_POINT` (R3 lift), `NtQueryInformationFile(FileStandardInformation)` (R4 lift), `CreateFile/CREATE_ALWAYS = FILE_SUPERSEDE`). Stage 4 (R5 lift): exact `CoInitializeEx → CoCreateInstance(CLSCTX_LOCAL_SERVER, CFAPI broker CLSID, IID_IUnknown)` sequence — the real RedSun activation API surface. |
| **2b** | Identifier Fidelity (0–2) | **1.5** | Real CFAPI Sync Root Manager CLSID `{829BD7DA-5F60-4B8B-A82C-C7B4DF0E2DB8}` used in stage 4. Real `TieringEngineService` queried for pre-check state. Sandbox file naming (`FakeTarget.exe`) does not yet match real targets like `TieringEngineService.exe` — pure-sandbox naming is intentional under the safety gate. |
| **2c** | Detection-Rule Firing (0–2) | **0.5 (capped)** | Pre-lab cap. Rule-to-primitive mapping documented in *Detection Opportunity Audit* below. Four stages × ~5 rule groups means ≥80% firing should reach 2.0 after lab re-run. |
| **3a** | Schema & Metadata | **1.0** | Schema v2.0 InitLogger + complete metadata header (with new T1559.001 + execution tactic) + org UUID + signed. RubricVersion: "v2". |
| **3b** | Documentation Completeness | **1.0** | README + info.md (with this scorecard) + references.md present. MITRE mapping cites T1211, T1006, T1574, T1559.001 official IDs. |
| **3c** | Logging & Plumbing | **1.0** | test_logger v2.0 + per-stage `bundle_results.json` fan-out (R1, in place since v1) + `test_execution_log.json` + pre/post `system_snapshot_{pre,post}.json` (Defender status + ASR rules + AV exclusions + hotfix count + TieringEngineService state). |
| | **Total (capped)** | **7.0 / 10** | Realism: 4.7 / 7. Structure: 3.0 / 3. **Post-lab projection: 9.0 / 10**. |

## What changed in v2 vs v1

| Lift | Effect |
|---|---|
| **R1** — Per-stage bundle_results.json fan-out | Already in place since v1 — re-confirmed |
| **R2** — Enrich bundle events with Win32 API parameters | Existing LogMessage calls already capture API parameters in human-readable form. Marked satisfied at current verbosity; future schema lift would add structured per-API-call telemetry |
| **R3** — `FSCTL_GET_REPARSE_POINT` read-back after creating reparse | New telemetry signal: reparse-enumerate detection rules now fire alongside reparse-create rules |
| **R4** — `NtQueryInformationFile(FileStandardInformation)` pre-race probe | Mirrors PoC's pre-supersede probe; adds NT-API telemetry |
| **R5** — New stage 4: `CoCreateInstance` against CFAPI broker CLSID with TieringEngineService pre-check | New COM-broker activation telemetry surface — the API signature RedSun's PoC actually exercises. Hard pre-check guard prevents service-start on systems where the service isn't already stopped |
| **X1** — Pre/post `system_snapshot_*.json` capture (incl. TieringEngineService state) | Pushes Logging dimension to 1.0 |
| **X3** — Detection-opportunity audit (below) | Cross-references generated rules with primitives |

## MITRE ATT&CK

### Techniques (v2)
- **T1211** — Exploitation for Defense Evasion (stages 1, 2)
- **T1006** — Direct Volume Access (stage 2)
- **T1574** — Hijack Execution Flow (stage 3)
- **T1559.001** — Inter-Process Communication: Component Object Model (stage 4 — new in v2)

### Tactics
- Defense Evasion (TA0005)
- Collection (TA0009)
- Persistence (TA0003)
- Execution (TA0002) — new in v2

## Architecture

Multi-stage killchain (4 stages, gzip-compressed embedded binaries).

### Stage Breakdown

| Stage | Technique | Binary | Description |
|---|---|---|---|
| 1 | T1211 | `<uuid>-T1211.exe.gz` | Cloud Files sync-root registration (`F0RT1KA-REDSUNSIM` provider) + EICAR drop in sandbox dir |
| 2 | T1006 | `<uuid>-T1006.exe.gz` | `NtOpenDirectoryObject(\Device)` + `NtQueryDirectoryObject` loop filtered for `HarddiskVolumeShadowCopy*` + `FSCTL_REQUEST_BATCH_OPLOCK` on sandbox file |
| 3 | T1574 | `<uuid>-T1574.exe.gz` | `FSCTL_SET_REPARSE_POINT` mount-point reparse (sandbox→sandbox) + **`FSCTL_GET_REPARSE_POINT` read-back (R3)** + **`NtQueryInformationFile(FileStandardInformation)` pre-race probe (R4)** + `CREATE_ALWAYS`/SUPERSEDE race loop against sandbox file |
| 4 | T1559.001 | `<uuid>-T1559.001-com.exe.gz` | **TieringEngineService state pre-check via `sc.exe query` — only proceeds if STOPPED. Then `CoInitializeEx + CoCreateInstance(CLSCTX_LOCAL_SERVER, {829BD7DA-…}, IID_IUnknown)` for activation telemetry. Post-activation state verification.** |

## Primitive-to-PoC Mapping

| F0RT1KA Stage | RedSun PoC Source (C++) |
|---|---|
| Stage 1 — `CfRegisterSyncRoot` | `DoCloudStuff()` in `RedSun.cpp:449–512` |
| Stage 1 — EICAR drop | `RedSun.cpp:692–695` (reversed EICAR via `rev()`) |
| Stage 2 — `NtOpenDirectoryObject` / `NtQueryDirectoryObject` | `RetrieveCurrentVSSList()` in `RedSun.cpp:143–262` + `ShadowCopyFinderThread` at `RedSun.cpp:267–434` |
| Stage 2 — `FSCTL_REQUEST_BATCH_OPLOCK` | `ShadowCopyFinderThread` at `RedSun.cpp:411–417`; also `RedSun.cpp:753–758` |
| Stage 3 — `FSCTL_SET_REPARSE_POINT` mount point | `RedSun.cpp:795–810` |
| Stage 3 — `FSCTL_GET_REPARSE_POINT` read-back **(v2 R3)** | implicit via reparse-handle lifecycle in PoC; explicit here for telemetry |
| Stage 3 — `NtQueryInformationFile(FileStandardInformation)` **(v2 R4)** | `RedSun.cpp:824–831` (pre-race file metadata probe) |
| Stage 3 — `FILE_SUPERSEDE` loop | `RedSun.cpp:835–847` |
| Stage 4 — `CoCreateInstance(SyncRootManager CLSID)` **(v2 R5)** | `RedSun.cpp:865–890` (COM activation of CFAPI broker; PoC uses this to trigger TieringEngineService-mediated rewrite). **F0RT1KA never replaces a real file via this activation** — only the activation telemetry is emitted, and only when the broker service is stopped. |

## Safety Deviations from the Real PoC

| Real PoC Behavior | F0RT1KA Substitute | Why |
|---|---|---|
| Sync root under `%TEMP%\RS-{GUID}` | Sync root under `c:\Users\fortika-test\RedSunSandbox` | `ARTIFACT_DIR` is the F0RT1KA-standard sandbox |
| Reparse target = `C:\Windows\System32` | Reparse target = `c:\Users\fortika-test\RedSunSandbox\ReparseTarget` | Hard boundary: never reparse to real system path |
| Supersede target = `TieringEngineService.exe` under System32 | Supersede target = `FakeTarget.exe` under sandbox | Hard boundary: no real system-file replacement |
| Provider name = `SERIOUSLYMSFT` | Provider name = `F0RT1KA-REDSUNSIM` | Defender-distinguishable in telemetry |
| Supersede loop = 1000 iterations | Supersede loop = 20 iterations | Exercises primitive without DoS |
| Oplock held until global event fires | Oplock released within stage function | Bounded oplock lifetime |
| `CoCreateInstance` always called | **Pre-checked: only fires when TieringEngineService is STOPPED** | Hard guard against driving service-start behavior |
| Waits up to 120 seconds on `gevent` | No cross-stage synchronization | No threads left running outside stage boundaries |

## Detection Opportunity Audit (v2 X3 lift)

For each detection rule the repo generates, the primitive that triggers it:

| Rule | File | Triggering primitive | Stage |
|---|---|---|---|
| `RedSun_NonVendor_CloudFiles_SyncRoot` | `_detections.kql`, `_sigma_rules.yml` | `CfRegisterSyncRoot` from non-OneDrive/iCloud/GoogleDrive | 1 |
| `RedSun_EICAR_Drop_In_SyncRoot` | `_rules.yar` | EICAR content under a sync-root directory | 1 |
| `RedSun_NtOpenDirectoryObject_Device` | `_detections.kql`, `_dr_rules.yaml` | `\Device` enumeration from non-system process | 2 |
| `RedSun_Batch_Oplock_Standard_User` | `_dr_rules.yaml`, `_detections.kql` | `FSCTL_REQUEST_BATCH_OPLOCK` from user-mode | 2 |
| `RedSun_FSCTL_Set_Reparse_Point` | `_detections.kql`, `_sigma_rules.yml` | `FSCTL_SET_REPARSE_POINT` setting `IO_REPARSE_TAG_MOUNT_POINT` from non-system process | 3 |
| `RedSun_FSCTL_Get_Reparse_Point_Probe` | `_detections.kql` | `FSCTL_GET_REPARSE_POINT` read-back **(R3 lift)** | 3 |
| `RedSun_NtQueryInformationFile_PreRace` | `_detections.kql` | `NtQueryInformationFile(FileStandardInformation)` followed by FILE_SUPERSEDE loop **(R4 lift)** | 3 |
| `RedSun_FILE_SUPERSEDE_Loop` | `_detections.kql`, `_dr_rules.yaml` | High-rate `CREATE_ALWAYS` against the same target | 3 |
| `RedSun_CFAPI_Broker_CoCreateInstance` | `_detections.kql`, `_sigma_rules.yml` | `CoCreateInstance` against CLSID `{829BD7DA-…}` from non-system process **(R5 lift)** | 4 |
| `RedSun_TieringEngineService_State_Probe` | `_detections.kql` | `sc.exe query TieringEngineService` immediately preceding COM activation **(R5 lift)** | 4 |

(Rule names are illustrative — exact names live in the rule files.)

## Artifact Inventory

During execution, the following artifacts are created and cleaned up under
`c:\Users\fortika-test\RedSunSandbox\`:

- `FakeTarget.exe` (EICAR + supersede target; removed at stage end)
- `OplockTarget.dat` (oplock target; removed at stage end)
- `ReparseSource/` (reparse source dir; removed at stage end)
- `ReparseTarget/` (reparse target dir; persistent empty dir)

Logs are written under `c:\F0\`:

- `test_execution_log.json` (Schema v2.0)
- `test_execution_log.txt` (human-readable)
- `bundle_results.json` (per-stage ES fan-out)
- `T1211_output.txt`, `T1006_output.txt`, `T1574_output.txt`, `T1559.001_output.txt` (per-stage stdout capture)
- `<uuid>_system_snapshot_pre.json` / `<uuid>_system_snapshot_post.json` **(v2 X1 lift)**

## Build & Run

```bash
# Build
cd tests_source/intel-driven/0d7e7571-45e2-426a-ac8e-bdb000439761/
./build_all.sh

# Deploy
scp build/0d7e7571-45e2-426a-ac8e-bdb000439761/0d7e7571-45e2-426a-ac8e-bdb000439761.exe target:C:\

# Execute
C:\0d7e7571-45e2-426a-ac8e-bdb000439761.exe
```

## Exit Code Semantics

| Exit | Meaning |
|---|---|
| 101 | All 4 stages completed unimpeded — endpoint is UNPROTECTED against the RedSun primitive chain |
| 126 | At least one stage was blocked/flagged by EDR — endpoint PROTECTED |
| 105 | A stage binary was quarantined on extraction |
| 999 | Prerequisite failure (e.g., Cloud Files feature not installed) |

Stage 4 will short-circuit cleanly with success code 0 if `TieringEngineService` is in any state other than STOPPED — the pre-check guard fires before any COM activation. The state-query telemetry is the value of the stage in that case.
