# UnDefend - Defender Signature/Engine Update DoS via File-Lock Race

## Identity
- **UUID**: `6a2351ac-654a-4112-b378-e6919beef70d`
- **Category**: intel-driven
- **Subcategory**: defender-evasion
- **Target**: windows-endpoint
- **Severity**: high
- **Complexity**: medium
- **Threat Actor**: Nightmare-Eclipse
- **Architecture**: multi-stage (3 stages)
- **Author**: sectest-builder
- **Created**: 2026-04-24
- **Re-scored under v2**: 2026-04-25
- **Re-scored under v2.1**: 2026-04-25 (Option B — see `docs/PROPOSED_RUBRIC_V2.1_SIGNAL_QUALITY.md`)
- **Version**: 2.0.0
- **Rubric version**: v2.1 (tiered, realism-first; signal-quality-not-tenant-defense)

## Test Score: 9.0/10

### Score Breakdown (Rubric v2.1)

| Tier | Sub-dimension | Score | Rationale |
|---|---|---|---|
| **1** | Safety Gate | **PASS** | All file writes confined to `ARTIFACT_DIR` / `LOG_DIR`. **No real Defender file is ever opened with lock or delete intent** — the v2 lifts (U1, U2, U3) added Defender-pattern naming, runtime registry reads, and a DELETE_PENDING primitive, but every operation still targets sandbox files the test creates. Real `ProductAppDataPath` is read from registry as TELEMETRY only — the sandbox subdir is still under `ARTIFACT_DIR`. ETW emission (U4) writes to the Application event log under a registered F0RT1KA source — synthetic events only. Service subscription torn down at return. All locks released in-function. Stage 3 (svcnotify) never starts/stops services. |
| **2a** | API Fidelity (0–2.5) | **2.4** | Stage 1 (T1083): exact `RegOpenKeyEx → RegQueryValueEx(ProductAppDataPath/SignatureLocation) → ReadDirectoryChangesW` sequence. Stage 2 (T1562.001-lock): exact `CreateFile(GENERIC_READ\|SYNCHRONIZE) → LockFile → LockFileEx(LOCKFILE_EXCLUSIVE_LOCK)` plus U3 `CreateFile(DELETE\|SYNCHRONIZE) → NtSetInformationFile(FileDispositionInformation, DeleteFile=TRUE)` — UnDefend's complete file-replacement API surface. Stage 3 (T1562.001-svcnotify): exact `OpenSCManager → OpenService → NotifyServiceStatusChangeW(WinDefend, SERVICE_NOTIFY_STOPPED)` with overlapped event. Strongest of triad. Adjusted to v2.1 max 2.5; v2 score 2.9/3 maps to 2.4/2.5. |
| **2b** | Identifier Fidelity (0–1.5) | **1.4** | Sandbox files literally named `mpavbase.vdm` and `mpengine.dll` (U1). Real `HKLM\SOFTWARE\Microsoft\Windows Defender\ProductAppDataPath` read at runtime (U2); sandbox subdir layout `Definition Updates\` mirrors real Defender layout. Real `WinDefend` service name used in stage 3. Score capped just below max because the sandbox `Definition Updates\` directory lives under `ARTIFACT_DIR` not `ProgramData\Microsoft\Windows Defender\` (deliberate safety choice). Adjusted to v2.1 max 1.5; v2 score 1.8/2 maps to 1.4/1.5. |
| **2c** | Telemetry Signal Quality (0–2) | **1.5** | Criterion 1 (signal richness) ✅ — every stage emits multiple identifiable signals across registry, file system, ETW, and service-control surfaces; U4 lift adds explicit ETW correlation events at stage boundaries. Criterion 2 (sensor mapping) ✅ — Detection Opportunity Audit table maps every primitive to KQL/Sigma/D&R rule shapes. Criterion 3 (rule-artifact validity) ✅ — bundled rule files parse cleanly. Criterion 4 (lab execution — all stages reached) ✅ — lab run on 2026-04-25 against WinDefender 1.449.291.0 reached all 3 stages with exit 101 (97s wallclock, gap-confirming run with no intervention). Sub-score 1.5 reflects passing all four criteria; remaining 0.5 reserved for tenant-defense audit which is explicitly out of scope per v2.1. |
| **2d** | Execution-Context Fidelity (0–1) | **0.7** | U4 ETW source registration degrades gracefully when running as standard user (registration fails non-fatally; remaining stages continue without ETW correlation events). Registry reads (U2) succeed for any user. Service notification (stage 3) succeeds without elevation since `NotifyServiceStatusChangeW` only requires SERVICE_QUERY_STATUS. Partial branching present but full per-context behavior switching not implemented. info.md notes user/SYSTEM context expectations. |
| **3a** | Schema & Metadata (0–1) | **1.0** | Schema v2.0 InitLogger + complete metadata header + org UUID + signed. RubricVersion: "v2.1". |
| **3b** | Documentation Completeness (0–1) | **1.0** | README + info.md (with this scorecard) + references.md present. MITRE mapping cites T1562.001, T1083 official IDs. |
| **3c** | Logging & Plumbing (0–0.5) | **0.5** | test_logger v2.0 + per-stage `bundle_results.json` fan-out + `test_execution_log.json` + pre/post `system_snapshot_{pre,post}.json` (Defender status + ASR rules + AV exclusions + hotfix count + WinDefend service state) + U4 synthetic ETW correlation events at every stage boundary. |
| **3d** | Operational Hygiene (0–0.5) | **0.5** | Orchestrator 9.4 MB ✅ (< 25 MB). Stage binaries each < 5 MB ✅. Per-stage watchdog goroutine via `time.AfterFunc` in `executeStage` ✅ (U5 lift, 2026-04-25 — kills the cmd if `Budget` exceeded; logs `watchdog_fired` and returns exit 999 around the existing ETW correlation markers). Per-stage execution-time budgets documented below ✅ (60s/30s/60s vs lab-observed 14s/2s/16s). `Endpoint.Stop()` exits cleanly within 30s on every code path including watchdog-triggered termination ✅. |
| | **Total** | **9.0 / 10** | Realism: 6.0 / 7.0. Structure: 3.0 / 3.0. Lab-verified end-to-end (3/3 stages reached, gap-confirming). |

### Stage Timeout Budgets (v2.1 §3d Operational Hygiene)

Per-stage execution-time budgets enforced by the orchestrator's `time.AfterFunc` watchdog (see `executeStage` in `6a2351ac-654a-4112-b378-e6919beef70d.go`). Budgets are sized at ~5–10× lab-observed runtime to leave headroom for slow scheduler stalls without permitting genuine hangs. If a stage exceeds its budget, the watchdog kills the process, logs `watchdog_fired after <duration>`, emits the existing `stage_N_end` ETW marker, and the orchestrator returns exit 999 for that stage.

| Stage | Technique | Budget | Lab-observed | Reason |
|---|---|---|---|---|
| 1 | T1083 registry recon + ReadDirectoryChangesW | 60 s | ~14 s | Registry reads + ReadDirectoryChangesW with FILE_FLAG_OVERLAPPED — async IO can stall under heavy AV inspection |
| 2 | T1562.001-lock | 30 s | ~2 s | LockFileEx is synchronous and fast; budget is generous to absorb scheduler stalls |
| 3 | T1562.001-svcnotify | 60 s | ~16 s | OpenSCManager + NotifyServiceStatusChangeW with overlapped event; SCM can be slow under load |

## What changed in v2 vs v1

| Lift | Effect |
|---|---|
| **U1** — Defender-pattern filenames (`mpavbase.vdm`, `mpengine.dll`) under sandbox `Definition Updates\` subdir | Path-based detection rules now fire alongside behavioral rules |
| **U2** — Read real `ProductAppDataPath` from `HKLM\SOFTWARE\Microsoft\Windows Defender` at runtime | Mirrors UnDefend's discovery flow exactly; registry-read telemetry |
| **U3** — `NtSetInformationFile(FileDispositionInformation, DeleteFile=TRUE)` against secondary sandbox file | New primitive — DELETE_PENDING race telemetry surface (UnDefend's delete-rename race) |
| **U4** — Synthetic ETW correlation events at each stage boundary via Application event log | SIEM-ingestion property — test boundaries become correlatable from log-collector side |
| **X1** — Pre/post `system_snapshot_*.json` capture (incl. WinDefend service state) | Pushes Logging dimension to 1.0 |
| **X3** — Detection-opportunity audit (below) | Cross-references generated rules with primitives |

## Threat Context

**UnDefend** was published by Nightmare-Eclipse as a PoC demonstrating that a
**standard-user process** (no admin required, no driver, no handle hijack) can
indefinitely disable Microsoft Defender signature and engine updates by racing
Windows' own file replace operations.

### Attack Preconditions

| Precondition | Required |
|---|---|
| Code execution as any user (SYSTEM not required) | Yes |
| Read access to HKLM\SOFTWARE\Microsoft\Windows Defender | Yes (standard-user default) |
| Read access to `C:\ProgramData\Microsoft\Windows Defender\Definition Updates` | Yes (standard-user default) |
| Ability to call `CreateFile` / `NtCreateFile` | Yes (universal) |
| Admin / SYSTEM / TrustedInstaller | **No** |

### Real-World Impact

A successful UnDefend run leaves the endpoint with:
- Stale Defender signature database (no new-threat detection coverage)
- Silent signature-update failures (no obvious error in Defender UI)
- During major platform updates, a non-functional engine (`mpavbase.vdm` locked)

## Simulation Architecture

### Why Multi-Stage

UnDefend's three attack vectors are distinct detection surfaces:
- **Stage 1 (T1083)**: Registry + directory watcher recon — visible at registry-read and FS-handle-open time
- **Stage 2 (T1562.001)**: File-lock primitive — visible at `CreateFile`/`LockFile`/`LockFileEx` time
- **Stage 3 (T1562.001)**: Service-notification subscription — visible at `NotifyServiceStatusChangeW` time

A monolithic binary would conflate these signals; a multi-stage architecture gives
detection-engineering precision (each stage is independently observable and
attributable via `bundle_results.json`).

### Stage Details

**Stage 1 — Defender Update-Path Discovery (T1083)**
1. Opens `HKLM\SOFTWARE\Microsoft\Windows Defender`, reads `ProductAppDataPath`.
2. Opens `HKLM\SOFTWARE\Microsoft\Windows Defender\Signature Updates`, reads `SignatureLocation`.
3. Calls `CreateFile(DefinitionUpdatesDir, FILE_LIST_DIRECTORY|SYNCHRONIZE, ..., FILE_FLAG_BACKUP_SEMANTICS)`.
4. Calls `ReadDirectoryChangesW(handle, ..., recursive=true, FILE_NOTIFY_CHANGE_SIZE, &ov)`.
5. Waits up to 2s for a change notification; cancels I/O and closes handle.

**Stage 2 — UnDefend File-Lock Primitive (T1562.001)**
1. Creates `ARTIFACT_DIR\UnDefendSim\sig-sample.yaml` (benign content).
2. Opens it with `CreateFile(GENERIC_READ|SYNCHRONIZE, FILE_SHARE_WRITE|FILE_SHARE_DELETE)`.
3. Calls `LockFile(handle, 0, 0, sizeLow, sizeHigh)` — acquires exclusive byte-range lock.
4. Releases with `UnlockFile`.
5. Re-opens and invokes `LockFileEx(handle, LOCKFILE_EXCLUSIVE_LOCK, 0, sizeLow, sizeHigh, &ov)`.
6. Releases with `UnlockFileEx`, deletes the sandbox file.

**Stage 3 — WinDefend Service-Stop Notification Subscription (T1562.001)**
1. `OpenSCManager(NULL, NULL, SC_MANAGER_CONNECT)`.
2. `OpenService(scm, "WinDefend", SERVICE_QUERY_STATUS|SERVICE_QUERY_CONFIG)`.
3. `QueryServiceStatus` — confirms `SERVICE_RUNNING`.
4. `NotifyServiceStatusChangeW(svc, SERVICE_NOTIFY_STOPPED, &notify)`.
5. Returns immediately (no `SleepEx` — callback guaranteed not to fire).
6. Subscription torn down when service handle closes at function return.

## MITRE ATT&CK Mapping

| Technique | Tactic | Why |
|---|---|---|
| [T1562.001 — Impair Defenses: Disable or Modify Tools](https://attack.mitre.org/techniques/T1562/001/) | defense-evasion | The `LockFile`/`LockFileEx` primitive (stage 2) and the `WinDefend` service-stop subscription (stage 3) are both components of UnDefend's Defender-disabling chain. |
| [T1083 — File and Directory Discovery](https://attack.mitre.org/techniques/T1083/) | discovery | Stage 1 reads Defender registry paths and subscribes to directory changes on `Definition Updates` — classic reconnaissance of a security product's operational filesystem. |

### Techniques Considered and Rejected

- **T1489 (Service Stop)** — UnDefend does not stop any service; it only *subscribes* to stop notifications. Mapping rejected.
- **T1055 (Process Injection)** — no injection of any kind. Rejected.
- **T1112 (Modify Registry)** — only registry READS, no writes. Rejected.

## Detection Summary

See `6a2351ac-654a-4112-b378-e6919beef70d_detections.kql`,
`_rules.yar`, `_sigma_rules.yml`, `_elastic_rules.ndjson`, `_dr_rules.yaml`.

Highest-fidelity behavioral signals:
1. **Non-admin process calling `NotifyServiceStatusChangeW` on `WinDefend`** — this pattern has essentially no legitimate use outside of security products themselves.
2. **`ReadDirectoryChangesW` handle opened on `ProgramData\Microsoft\Windows Defender\Definition Updates`** by a non-Microsoft-signed process.
3. **Registry queries for `ProductAppDataPath` + `SignatureLocation` under `HKLM\SOFTWARE\Microsoft\Windows Defender`** from a process that subsequently calls `LockFile`/`LockFileEx`.

### Detection Opportunity Audit (v2 X3 lift)

For each detection rule the repo generates, the primitive that triggers it:

| Rule | File | Triggering primitive | Stage |
|---|---|---|---|
| `UnDefend_Defender_RegKey_Reads` | `_detections.kql`, `_sigma_rules.yml` | Registry queries for `ProductAppDataPath` / `SignatureLocation` from non-Microsoft process | 1, 2 |
| `UnDefend_ReadDirectoryChangesW_Defender_Path` | `_detections.kql`, `_dr_rules.yaml` | `ReadDirectoryChangesW` handle on `Definition Updates\` by non-Microsoft process | 1 |
| `UnDefend_Mimikatz_Filename_Path` ⭐ NEW | `_rules.yar`, `_sigma_rules.yml` | File named `mpavbase.vdm` or `mpengine.dll` outside Defender's owned path **(U1 lift)** | 2 |
| `UnDefend_LockFile_NonAdmin` | `_detections.kql`, `_dr_rules.yaml` | `LockFile`/`LockFileEx` from standard-user process targeting any path matching Defender naming | 2 |
| `UnDefend_FileDispositionInformation_DeletePending` ⭐ NEW | `_detections.kql`, `_dr_rules.yaml` | `NtSetInformationFile(FileDispositionInformation, DeleteFile=TRUE)` from non-admin user **(U3 lift)** | 2 |
| `UnDefend_NotifyServiceStatusChangeW_WinDefend` | `_detections.kql`, `_sigma_rules.yml` | Non-admin process subscribing to `WinDefend` state change | 3 |
| `UnDefend_ETW_Synthetic_Marker` ⭐ NEW (correlation aid) | (synthetic, not a "real" rule but correlation hook) | F0RT1KA event ID 7777 in Application log under source `F0RT1KA-UnDefend-Sim` **(U4 lift)** | all |

(Rule names are illustrative — exact names live in the rule files.)

## Defense Summary

See `6a2351ac-654a-4112-b378-e6919beef70d_DEFENSE_GUIDANCE.md` and
`6a2351ac-654a-4112-b378-e6919beef70d_hardening.ps1`.

Key controls:
- Enable Defender tamper protection (attack still works without admin, but tamper protection blocks related escalation).
- ASR rule: Block untrusted and unsigned processes that run from USB (and general ASR hardening).
- Monitor process→Defender-service interactions (EDR custom rule).
- Accelerated signature-update failure alerting (Defender has ETW events for failed updates).

## Source Provenance

- **Primary source**: https://github.com/Nightmare-Eclipse/UnDefend
- **Research files consulted**: `EXPLOIT_EXPLAINED.md`, `UnDefend.cpp`
- **Negative constraints honored**: `AGENTS.md` and `UnDefend_Sim.cpp` deliberately not read
- **See also**: `6a2351ac-654a-4112-b378-e6919beef70d_references.md`
